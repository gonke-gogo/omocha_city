  <div class="card-body">
      <h4 class="card-title fw-bold"><%= link_to user.name, user_path(user) %></h4>
      <td>
      <%= link_to others_profile_path(user) do %>
        <%= image_tag user.avatar.url, class: 'rounded-circle', size: '100x100' %>
      <% end %>
      </td>
      <p><%= render 'follow_relationships/follow_button', user: user %></P>
    </div>